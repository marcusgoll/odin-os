version: 1

# Telemetry runtime settings now come from code-level defaults and explicit
# runtime services. This file remains only as a validated bootstrap marker so
# stale undocumented keys fail fast instead of silently drifting.
