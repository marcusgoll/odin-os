# Stage 7 Supervised E2E Docs Proof

Date: 2026-05-01
Project: odin-core
Repo: marcusgoll/odin-os
Issue: #104 Stage 7 supervised E2E docs proof 1777659470904949149

## Current State

This checkout has the core Odin CLI surface and the general verification contract, but it does not currently contain the Stage 7 local E2E runner surface.

The repo was audited before this file was added. The worktree was clean before the docs-only addition, and no runner, security, workspace, token, deploy, CI, scheduler, PR, or merge behavior was changed.

## What Already Exists

- `Makefile` defines the current local gates: `format`, `fmtcheck`, `lint`, `test`, `test-alpha`, `test-media`, `test-skills`, `ci`, `build`, `install-local`, and `uninstall-local`.
- `cmd/odin/main.go` resolves the Odin repo root and dispatches into `internal/app/lifecycle.Run`.
- `internal/app/lifecycle/run.go` exposes the current top-level command set: `help`, `repl`, `overview`, `doctor`, `healthcheck`, `serve`, `backup`, `restore`, `verify-backup`, `status`, `legacy`, `project`, `workspace`, `scope`, `jobs`, `runs`, `approvals`, `intake`, `agenda`, `logs`, `task`, `initiative`, `companion`, `profile`, `followup`, `transition`, and `skills`.
- `docs/contracts/verification-model.md` defines the active rule that operator-visible behavior must be proven through the real Odin command path and that internal tests alone are not sufficient.
- `odin doctor --json` is available and produced a healthy structured report with an isolated `ODIN_ROOT`.

## Gaps

- `make odin-e2e-local` is missing in this checkout.
- `odin e2e --help` is missing in this checkout and returns `unknown command: e2e`.
- No `fixtures/e2e/` directory, `scripts/odin-e2e-local.sh`, `.odin/e2e/` contract, or Stage 7 E2E report artifact path exists in the tracked repo state audited for this task.
- Because the E2E command surface is absent, Stage 7 supervised E2E cannot be claimed complete from this checkout.

## Reuse Plan

The correct reuse path remains the existing Odin CLI and verification model:

- Keep command-level proof on the repo-owned `odin` command path.
- Reuse `doctor --json` for runtime readiness proof.
- Treat `make odin-e2e-local` as the intended local E2E entrypoint once it exists in this checkout.
- Do not bypass the missing E2E command by calling lower-level runtime services directly.

## New Additions

Only this operations report was added:

- `docs/operations/stage-7-supervised-e2e-2026-05-01-1777659470904949149.md`

No code, runner, security, workspace, token, deploy, CI, scheduler, PR, or merge files were changed.

## Why New Additions Are Necessary

Issue #104 asks for a Stage 7 supervised E2E docs proof. Since the current checkout lacks the required E2E make target and `odin e2e` command surface, the honest output is a durable operations note that records the audit, the proof commands, the hard blockers, and the operating rule for the next slice.

## Real odin E2E Verification

Commands run from the repo root:

```bash
make odin-e2e-local
```

Result:

```text
make: *** No rule to make target 'odin-e2e-local'.  Stop.
```

```bash
GOCACHE="$(mktemp -d /tmp/odin-gocache-XXXXXX)" ODIN_ROOT="$(mktemp -d)" go run ./cmd/odin help
```

Result:

```text
Usage: odin <command> [args]

Commands: help repl overview doctor healthcheck serve backup restore verify-backup status legacy project workspace scope jobs runs approvals intake agenda logs task initiative companion profile followup transition skills
```

```bash
GOCACHE="$(mktemp -d /tmp/odin-gocache-XXXXXX)" ODIN_ROOT="$(mktemp -d)" go run ./cmd/odin doctor --json
```

Result: exited 0 and returned `status: healthy` with healthy checks for `database`, `registry`, `executor`, `queue`, `projections`, `source_freshness`, and `workspace_prerequisites`.

```bash
GOCACHE="$(mktemp -d /tmp/odin-gocache-XXXXXX)" ODIN_ROOT="$(mktemp -d)" go run ./cmd/odin e2e --help
```

Result:

```text
unknown command: e2e
exit status 1
```

The default Go build cache path was read-only in this sandbox, so the Odin command probes used a temporary `GOCACHE` under `/tmp`. That did not change repo files.

## Remaining Risks

- Stage 7 supervised E2E is unproven in this checkout because the local E2E target and command are absent.
- `doctor --json` proves basic runtime readiness only; it does not prove fixture-backed supervised E2E behavior.
- A future implementation slice must add or reconcile the missing E2E runner through the canonical Odin CLI path before Stage 7 can be marked complete.

## Best operating rule going forward

Treat `make odin-e2e-local` as a hard required proof target, not a warning-only convenience. If the target or the underlying `odin e2e` command is missing, stop and report the missing command surface instead of substituting internal service calls or unrelated tests.
