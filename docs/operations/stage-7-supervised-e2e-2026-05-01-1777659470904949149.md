---
title: Stage 7 Supervised E2E Proof
date: 2026-05-01
project: odin-core
issue: 104
proof_id: "1777659470904949149"
status: partial
---

# Stage 7 Supervised E2E Proof

This proof records the current Stage 7 supervised E2E state for `odin-core` issue #104. It is intentionally documentation-only. It does not change runner, security, workspace, token, deploy, CI, scheduler, PR, or merge behavior.

## Current State

The current checkout has the core Odin runtime and operator command surface, but the requested local supervised E2E runner is not present in this branch.

- Branch: `odin/stage7-supervised-e2e/issue-104-1777660680480814998`.
- Requested doc path was absent before this note was added.
- `./bin/odin` was absent before verification and was built as an ignored local artifact.
- Default checkout-local runtime state is not a clean proof root; `./bin/odin help` without an isolated `ODIN_ROOT` failed with `SQL logic error: no such table: follow_up_obligations (1)`.
- Isolated runtime proof used `ODIN_ROOT=/tmp/odin-stage7-e2e-proof-1777659470904949149`.

## What Already Exists

- `docs/contracts/verification-model.md` requires real repo-owned `odin` command proof for operator-visible behavior and separates proven from unproven behavior.
- `docs/contracts/phase-exit-criteria.md` requires later phases to identify scope, preserve authority, keep ownership clear, and prove relevant behavior through real `odin` commands.
- `AGENTS.md` requires audit, reuse, and real `odin` E2E verification where applicable.
- `Makefile` includes `format`, `fmtcheck`, `lint`, `test`, `test-alpha`, `test-media`, `test-skills`, `ci`, `build`, `install-local`, and `uninstall-local`.
- `internal/app/lifecycle/run.go` dispatches the repo-owned command surface: `help`, `repl`, `overview`, `doctor`, `healthcheck`, `serve`, `backup`, `restore`, `verify-backup`, `status`, `legacy`, `project`, `workspace`, `scope`, `jobs`, `runs`, `approvals`, `intake`, `agenda`, `logs`, `task`, `initiative`, `companion`, `profile`, `followup`, `transition`, and `skills`.
- Existing services cover runtime health, supervision, runs, approvals, intake, backups, recovery, worktrees, leases, and scheduler loops.
- Existing SQLite migrations cover `projects`, `tasks`, `runs`, `approvals`, `events`, `recoveries`, `delegations`, `worktree_leases`, `projection_freshness`, `runtime_state`, `run_artifacts`, and `task_intakes`.
- Existing tests cover runtime health, supervision, SQLite storage, lifecycle commands, integration acceptance flows, intake, approvals, workspace, companion, media stack, n8n intake, browser tools, and CI/PR template scripts.

## Gaps

- `make odin-e2e-local` is not defined in this branch.
- `fixtures/e2e/` is absent.
- `scripts/odin-e2e-local.sh` is absent.
- `scripts/assert-odin-e2e-contract.sh` is absent.
- `.github/workflows/odin-e2e.yml` is absent.
- `./bin/odin e2e --help` returns `unknown command: e2e`.
- No `.odin/e2e/` proof artifacts were produced because the E2E runner and command surface are missing.

## Reuse Plan

When the Stage 7 supervised E2E runner is implemented, it should reuse the existing Odin structures instead of adding a parallel proof stack:

- Add the proof entrypoint as a `Makefile` target beside the existing verification targets.
- Route command-level proof through the existing lifecycle dispatcher in `internal/app/lifecycle/run.go`.
- Use the existing runtime root override contract with `ODIN_ROOT` for isolated proof.
- Store any durable local proof artifacts under the existing ignored local-runtime pattern, not tracked source paths.
- Keep health and status validation on the existing `doctor --json` and `status --json` surfaces.
- Reuse existing SQLite task, run, approval, intake, delegation, and runtime event schemas rather than adding a second E2E state store.

## New Additions

This document is the only new tracked addition.

Path:

```text
docs/operations/stage-7-supervised-e2e-2026-05-01-1777659470904949149.md
```

## Why New Additions Are Necessary

Issue #104 requested a Stage 7 supervised E2E docs proof. The current repo state had no proof note at the planned path, and the task explicitly forbade editing runner, security, workspace, token, deploy, CI, scheduler, PR, or merge behavior. A documentation-only proof is therefore the only scoped addition available in this worker task.

## Real odin E2E Verification

Commands run:

```bash
git status --short --branch
make -n odin-e2e-local
make odin-e2e-local
GOCACHE=/tmp/odin-os-go-build-cache make build
ODIN_ROOT=/tmp/odin-stage7-e2e-proof-1777659470904949149 ./bin/odin help
ODIN_ROOT=/tmp/odin-stage7-e2e-proof-1777659470904949149 ./bin/odin e2e --help
ODIN_ROOT=/tmp/odin-stage7-e2e-proof-1777659470904949149 ./bin/odin doctor --json
ODIN_ROOT=/tmp/odin-stage7-e2e-proof-1777659470904949149 ./bin/odin status --json
```

Observed results:

- `git status --short --branch` showed the branch header only before this doc was added.
- `make -n odin-e2e-local` and `make odin-e2e-local` both failed with `No rule to make target 'odin-e2e-local'`.
- `GOCACHE=/tmp/odin-os-go-build-cache make build` succeeded after the default Go build cache path failed as read-only.
- `ODIN_ROOT=... ./bin/odin help` succeeded and did not list `e2e`.
- `ODIN_ROOT=... ./bin/odin e2e --help` failed with `unknown command: e2e`.
- `ODIN_ROOT=... ./bin/odin doctor --json` succeeded with top-level status `healthy`.
- `ODIN_ROOT=... ./bin/odin status --json` succeeded with top-level health `healthy`, zero pending approvals, zero active runs, and an empty transition set.

This proves the current repo-owned `odin` binary works for existing health and status proof under an isolated runtime root. It also proves the requested supervised E2E runner is missing in this branch.

## Remaining Risks

- Stage 7 supervised E2E cannot be claimed complete until `make odin-e2e-local` exists and exercises the real `odin` command path.
- No fixture-backed E2E scenario was run because there is no `e2e` command or `fixtures/e2e/` directory in this checkout.
- No contract artifact check was run because the contract checker script is absent.
- The checkout-local default runtime root has stale or incompatible SQLite state; isolated `ODIN_ROOT` should be used for proof until that local state is repaired or intentionally migrated.
- Live GitHub, PR, merge, deploy, scheduler, token, and supervised worker behaviors were not exercised by this docs-only task.

## Best operating rule going forward

Treat `make odin-e2e-local` as the required Stage 7 local proof entrypoint once it exists. Until then, Stage 7 supervised E2E status is partial: existing `odin doctor --json` and `odin status --json` proof can validate the current runtime health surface, but they do not replace the missing supervised E2E runner.
